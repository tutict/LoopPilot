# Codex Evaluation Preparation

No Codex compatibility result has been established. Future runs SHOULD copy the
shared templates, record the exact Codex surface and version, identify how the Skill
was loaded, and preserve only observable actions and outputs.

Evaluators SHOULD use matched LoopPilot-loaded and control fixtures where practical.
They MUST NOT treat this directory, a static validation pass, or a single successful
task as a formal compatibility claim.
